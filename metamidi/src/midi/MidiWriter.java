package midi;

import javax.sound.midi.MidiMessage;
import java.util.function.Consumer;

public class MidiWriter implements Consumer<MidiMessage> {



    @Override
    public void accept(MidiMessage midiMessage) {

    }
}
